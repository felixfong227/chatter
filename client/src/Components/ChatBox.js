import React from 'react';
import Input from '@material-ui/core/Input';

export default class ChatBox extends React.Component {
    render() {
        return (
            <Input defaultValue="It works!" />
        );
    }
}